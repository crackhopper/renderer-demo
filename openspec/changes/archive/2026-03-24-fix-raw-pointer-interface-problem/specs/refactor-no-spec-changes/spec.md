## No Spec Changes Required

This change is a pure refactoring task that does not introduce new capabilities or modify existing requirements. The C++ style guide rule being applied already exists in `openspec/specs/cpp-style-guide/spec.md`.

All changes are implementation-level and do not affect system behavior or requirements defined in any capability spec.
